<Window x:Class="WarehouseApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WarehouseApp" Height="700" Width="1200">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Бічне меню -->
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="#59595e"
                      Grid.Column="0">
            <StackPanel>
                <!-- Categories -->
                <Button 
                        Foreground="White"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        FontSize="16"
                        Padding="10"
                        HorizontalContentAlignment="Left"
                        Command="{Binding NavigateToCategoriesCommand}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Images/categories.png" Width="30" Height="30" Margin="0,0,10,0"/>
                        <TextBlock Text="Categories"
                           Foreground="White"
                           FontSize="16"
                           VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Products -->
                <Button 
                        Foreground="White"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        FontSize="16"
                        Padding="10"
                        HorizontalContentAlignment="Left"
                        Command="{Binding NavigateToProductsCommand}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Images/product.png" Width="30" Height="30" Margin="0,0,10,0"/>
                        <TextBlock Text="Products"
                           Foreground="White"
                           FontSize="16"
                           VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>


                <!-- Manufacturers -->
                <Button 
                        Foreground="White"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        FontSize="16"
                        Padding="10"
                        HorizontalContentAlignment="Left"
                        Command="{Binding NavigateToManufacturersCommand}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Images/manufacture.png" Width="30" Height="30" Margin="0,0,10,0"/>
                        <TextBlock Text="Manufacturers"
                           Foreground="White"
                           FontSize="16"
                           VerticalAlignment="Center"/>
                    </StackPanel>
                </Button> 

                <!-- Warehouses -->
                <Button 
                        Foreground="White"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        FontSize="16"
                        Padding="10"
                        HorizontalContentAlignment="Left"
                        Command="{Binding ToggleWarehousesMenuCommand}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Images/warehouse.png" Width="30" Height="30" Margin="0,0,10,0"/>
                        <TextBlock Text="Warehouses"
                           Foreground="White"
                           FontSize="16"
                           VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Підменю складів -->
                <ItemsControl ItemsSource="{Binding Warehouses}"
                              Visibility="{Binding IsWarehousesExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}"
                                    Foreground="#E0E0E0"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    FontSize="14"
                                    Padding="20,5,5,5"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding DataContext.SelectWarehouseCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Контент -->
        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>



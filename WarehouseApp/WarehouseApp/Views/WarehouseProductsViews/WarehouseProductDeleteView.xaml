<Window x:Class="WarehouseApp.Views.WarehouseProductsViews.WarehouseProductDeleteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseApp.Views.WarehouseProductsViews"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Delete Product"
        Height="250" Width="420">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Message}"
               FontSize="18"
               TextWrapping="Wrap"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               TextAlignment="Center"
               Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Delete" FontSize="16" Width="90" Height="40" Command="{Binding ConfirmCommand}" 
                Style="{StaticResource RoundedDeleteButtonStyle}"/>

            <Button Content="Cancel" FontSize="16" Width="90" Height="40" Command="{Binding CancelCommand}" 
                Style="{StaticResource RoundedDefaultButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>

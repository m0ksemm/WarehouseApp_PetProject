<Window x:Class="WarehouseApp.Views.WarehousesViews.WarehouseAddEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseApp.Views.WarehousesViews"
        mc:Ignorable="d"
        Title="Warehouse" Height="180" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Text="{Binding WarehouseName}"
                 FontSize="18"
                 Margin="0 0 0 20"/>

        <Button Grid.Row="1"
                Content="Save"
                FontSize="18"
                HorizontalAlignment="Right"
                Width="100"
                Command="{Binding SaveCommand}"/>
    </Grid>
</Window>
